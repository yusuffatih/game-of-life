#ifndef CONFIG_H
#define CONFIG_H

#define GRID_WIDTH 20
#define GRID_HEIGHT 20
#define WIDTH 800
#define HEIGHT 600

#define WIDTH_COUNT ((WIDTH) / (GRID_WIDTH))
#define HEIGHT_COUNT ((HEIGHT) / (GRID_HEIGHT))

#endif
